type Person {
  id: ID!
  name: String!
  description: String
  roll: [Roll] @relation(name: "PLAYING", direction: "OUT")
}

type Roll {
  id: ID!
  type: RollType!
  name: String!
  person: [Person] @relation(name: "PLAYING", direction: "IN")
}

enum RollType {
  STAR
  SUPPORT
  EXTRA
}